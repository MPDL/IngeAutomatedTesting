package test.java.base;

/**
 * Represents the genre groups as listed.
 * Make sure to keep them in sync with the data table,
 * if any changes are made.
 *
 */
public enum GenreGroup {
	SOURCE_DEP, SOURCE_DEP_BOOK, SOURCE_INDEP, NO_SOURCE, EVENT_DEPENDENT, EVENT, DEGREE, PATENT, LEGAL 
}
